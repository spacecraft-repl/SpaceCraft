config:
  target: "http://142.93.16.16:4000/"
  phases:
    - duration: 300
      arrivalRate: 1
scenarios:
  - engine: "socketio"
    flow:
      - emit:
          channel: "evaluate"
          data: { code: "[1,2,3].map(&:to_s)" }
          response:
            channel: "output"
      - think: 1
